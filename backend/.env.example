# Server Configuration
PORT=3001
NODE_ENV=development

# CORS: frontend origin(s) allowed to call the API (comma-separated for multiple)
# Production example: https://skyreachair.com
FRONTEND_URL=http://localhost:3000

# MongoDB Database
DB_CONN=your-mongodb-connection-string

# JWT Secret - Generate a strong random string
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Admin User (for initial setup via 'npm run create-admin')
# These values are used as defaults when creating the first admin user
# IMPORTANT: You MUST run 'npm run create-admin' after setting these
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=your-admin-password

# Email Configuration (Gmail SMTP)
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM=noreply@example.com
ADMIN_EMAIL_RECIPIENT=adminrecipient@example.com

# Alternative: Ethereal Email for testing
# Get credentials from https://ethereal.email
ETHEREAL_USER=
ETHEREAL_PASS=
