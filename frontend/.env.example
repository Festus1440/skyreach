# Backend API URL (required in production).
# In development this defaults to http://localhost:3001 if unset.
# When deploying, set this in your platform's environment variables (e.g. Coolify).
# Example for production: https://api.yourdomain.com
NEXT_PUBLIC_API_URL=http://localhost:3001

# Contact info shown on the site (header, footer, contact section, landing page).
NEXT_PUBLIC_PHONE=1-800-123-4567
NEXT_PUBLIC_EMAIL=service@skyreachair.com
